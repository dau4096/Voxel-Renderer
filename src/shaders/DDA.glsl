/* DDA.glsl */
//Will be added into other files that use `#include <DDA>` by a preprocessor step.


bool differentialDifferenceAnalysis(
	in vec3 origin, in vec3 direction
) {
	//Do DDA on some dataset.
	return false; //TBA
}
